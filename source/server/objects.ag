guard UserProperties: {
	email: string,
	passdata: string,
	username?: string
};

guard RoleProperties: {
	user_id: string,
	name: string
};

guard SessionProperties: ../api/server/State & {
	expires_utc: integer,
	wait_until_utc: integer,
	username?: string,
	username_attempts?: integer,
	email?: string,
	email_attempts?: integer,
	token_hash?: string,
	token_hash_attempts?: integer,
	passdata?: string,
	passdata_attempts?: integer,
	user_id?: string,
	ticket_hash?: string
};

guard OriginProperties: {
	address: string,
	expires_utc: integer,
	wait_until_utc: integer
};
